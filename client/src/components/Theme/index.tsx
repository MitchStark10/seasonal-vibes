import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#1DB954",
    },
    secondary: {
      main: "rgba(0, 0, 0, 0.87)",
    },
  },
  typography: {
    button: {
      fontWeight: "bold",
    },
  },
});
